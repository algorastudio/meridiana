# Archivio codice pre-refactor

Questa cartella contiene snapshot di file sostituiti dal refactor mixin
(v1.3.0) di `catasto_db_manager.py` e `gui_widgets.py`.

I file qui dentro **non vengono importati** dall'applicazione e sono
mantenuti solo come riferimento storico per consultazione manuale.

- `catasto_db_manager_old.py` — versione monolitica di `catasto_db_manager.py`
  prima dello split nei mixin in `db_modules/`.
- `gui_widgets_old.py` — versione precedente di `gui_widgets.py`.

Se non servono per consultazione, possono essere eliminati: la cronologia
git li conserva comunque.
