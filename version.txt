# UTF-8
#
# For more details about fixed file info 'ffi' see:
# http://msdn.microsoft.com/en-us/library/ms646997.aspx
VSVersionInfo(
  ffi=FixedFileInfo(
    # Versione del file e del prodotto (MAJOR, MINOR, PATCH, BUILD)
    filevers=(1, 2, 1, 0),
    prodvers=(1, 2, 1, 0),
    # Maschera per indicare quali campi sono validi
    mask=0x3f,
    # Flag vari (es. 0x0 per una build di release)
    flags=0x0,
    # Sistema operativo di destinazione
    OS=0x40004,
    # Tipo di file (0x1 per un'applicazione)
    fileType=0x1,
    # Sottotipo (non specificato)
    subtype=0x0,
    # Timestamp del file (lasciare a 0 per auto)
    date=(0, 0)
  ),
  kids=[
    StringFileInfo(
      [
      StringTable(
        '041004b0', # Codice lingua: Italiano (Italia), set di caratteri Unicode
        [StringStruct('CompanyName', 'Marco Santoro'),
         StringStruct('FileDescription', 'Meridiana - Archivio Catastale Storico'),
         StringStruct('FileVersion', '1.2.1'),
         StringStruct('InternalName', 'Meridiana'),
         StringStruct('LegalCopyright', u'Copyright © Marco Santoro. In gentile concessione gratuita all\'Archivio di Stato di Savona.'),
         StringStruct('OriginalFilename', 'Meridiana.exe'),
         StringStruct('ProductName', 'Meridiana'),
         StringStruct('ProductVersion', '1.2.1')])
      ]),
    VarFileInfo([VarStruct('Translation', [1040, 1200])]) # 1040 è LCID per Italiano
  ]
)
